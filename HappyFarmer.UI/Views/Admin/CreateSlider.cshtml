@model SliderModel
@{
    ViewData["Title"] = "CreateSlider";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<marquee direction=right>
    <h2 style="color:green;">Yeni Slider Ekleme Ekranı</h2>
</marquee>

<div class="row">
    <div class="col-md-10">
        @if (ViewBag.ErrorMessage != null)
        {
            <div class="text-danger">@ViewBag.ErrorMessage</div>
        }
        @if (@ViewBag.IncerrectImageExtension != null)
        {
            <div style="margin-left:225px;" class="alert alert-danger"><center>@ViewBag.IncerrectImageExtension</center></div>
        }
        <form asp-controller="Admin" asp-action="CreateSlider" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group row">
                <label asp-for="Name" class="col-md-2 col-form-label">Slider Adı</label>
                <div class="col-md-10">
                    <input type="text" name="Name" class="form-control" />
                </div>
            </div>

            <div class="form-group row">
                <label asp-for="ImagePath" class="col-md-2 col-form-label">Slider Resmi</label>
                <div class="col-md-10">
                    <input type="hidden" name="ImagePath" />
                    <input type="file" name="file" class="form-control" />
                </div>
            </div>

            <div class="form-group row">
                <label asp-for="SequenceNumber" class="col-md-2 col-form-label">Slider Sıra No</label>
                <div class="col-md-10">
                    <input type="number" name="SequenceNumber" class="form-control" />
                </div>
            </div>

            <div class="form-group row">
                <label asp-for="Situation" class="col-md-2 col-form-label">Slider Durum</label>
                <div class="col-md-10">
                    <select name="Situation" class="form-control">
                        <option value="true">Aktif</option>
                        <option value="false">Pasif</option>
                    </select>
                </div>
            </div>

            <div class="form-group row">
                <label asp-for="Url" class="col-md-2 col-form-label">Slider URL</label>
                <div class="col-md-10">
                    <input type="text" name="Url" class="form-control" />
                </div>
            </div>

            <div class="form-group row">

                <div class="col-md-10 offset-md-2">
                    <button style="width: 100%; margin-left:222px;" type="submit" class="btn btn-success">Slider Kaydet</button>
                </div>
            </div>
        </form>
    </div>
</div>
